//! CuprumFS tools — mkfs, fsck, mount helper
//! Работают на хосте (Linux/macOS) для создания образов.
//! Run on host (Linux/macOS) for creating disk images.

fn main() {
    println!("cuprumfs-tools — TODO");
}
